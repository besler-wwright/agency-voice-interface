# Core dependencies
aiohttp
pillow
pydantic
python-dotenv
loguru
rich
pygame  # For visual interface
pyaudio  # For audio handling
websockets  # For WebSocket communication

# Platform-specific dependencies
pywin32; sys_platform == 'win32'  # For Windows support
pyobjc-framework-Quartz; sys_platform == 'darwin'  # For macOS window management and screenshot capture

# Optional dependencies
agency-swarm  # For BaseTool functionality
